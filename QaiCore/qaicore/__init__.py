"""qaicore package (compat layer).

Historically, docs and agent prompts referenced `qaicore.tools`.
The canonical implementation lives in the top-level `tools` package.
This package re-exports it for backward-compatible imports.
"""
